pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
rootProject.name = "Reactive Currencies"
include(":app")
include(":presentation")
include(":domain")
include(":data")
